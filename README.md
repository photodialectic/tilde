# ~
My dot files repo.

## installation
`cd ~`

`git clone https://github.com/photodialectic/tilde`

`cd tilde`

`git submodule init`

`git submodule update`

`./install.sh`

## update

`./install.sh`

