define([
  "lodash",
  "base/base",
  "config",
  "app",
],

function(_, Base, Config, app) {

  /**
   * @BASENAME@
   *
   * @returns {undefined}
   */
  var @BASENAME@ = Base.View._.extend({
    events: {},

    initialize: function(options) {
    },


  });

  return @BASENAME@;

});
